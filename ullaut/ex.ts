function calculateResult(a: number, b: number, d: number): number {
    // Sum the provided values
    return a + b + d;
}

// Example usage:
const a: number = 5;
const b: number = 10;
const d: number = 15;

const result: number = calculateResult(a, b, d);
console.log(`The result is: ${result}`);
